name: hahah,这个111
on:
  workflow_dispatch: 
  push:
    branches: ['main']
  pull_request:
    branches: ['main', 'abc', 'def']
jobs:
  demo:
    name: haha111
    runs-on: ubuntu-latest
    steps:
      - run: echo ${{!startsWith('haha', 'hah')}} && echo ${{2 == '2'}}
