name: hahah,这个111
on:
  workflow_dispatch: 
  push:
    branches: ['main']
  pull_request:
    branches: ['main', 'dev']
jobs:
  demo:
    name: haha
    runs-on: ubuntu-latest
    steps:
      - run: echo 'aa' && echo ${{!startsWith('haha', 'hah')}} && echo ${{2 == '2'}}
